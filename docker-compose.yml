version: "3.9"
services:
  db:
    image: ankane/pgvector:latest
    container_name: letty_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: letty
      POSTGRES_PASSWORD: letty
      POSTGRES_DB: letty
    ports:
      - "5432:5432"
